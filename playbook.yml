- hosts: worker
  become: true

  tasks:

  - name: Pull Docker Image
    command: docker pull zaidmohammad038/static-web:latest

  - name: Run Docker Container
    command: docker run -d -p 8085:80 --name webapp zaidmohammad038/static-web:latest
